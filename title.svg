<svg width="100%" height="100" xmlns="http://www.w3.org/2000/svg">
  <text x="50%" y="70" text-anchor="middle"
        font-size="36" font-family="Segoe UI, sans-serif"
        font-weight="700" fill="#d452a0" opacity="0">

    🌸 Salut, moi c’est Yasmine Ben Nessib 🌸

    <!-- Fade-in -->
    <animate attributeName="opacity"
             from="0" to="1"
             dur="2s"
             fill="freeze" />

    <!-- Slide-up -->
    <animate attributeName="y"
             from="100" to="70"
             dur="2s"
             fill="freeze" />
  </text>
</svg>
